services:
  ecowitt-prom:
    build: .
    container_name: ecowitt-prom
    ports:
      - "8080:8080"
    environment:
      - LISTEN_ADDR=:8080
    networks:
      - monitoring
    restart: unless-stopped

networks:
  monitoring:
    external: true
